package oop;

    // Overloading
    // 적용 -> [OOP : 메서드, 생성자], [구조적 언어: 함수]
    // 키포인트
    // -> 함수, 메서드, 생성자에서 동일한 이름을 가지는 sub-routine

    class Opp250319 {
        // 1. 멤버 변수(O)
        // 2. 멤버 메서드(O)
        // 3. 생성자 (O)
        // 4. 초기화 함수 (O)
        // 5. 소멸자 (X)

    }

